name: Automated Ingestion
on:
  schedule:
    - cron: '0 3 1 */3 *'
  workflow_dispatch:
    inputs:
      force:
        description: 'Force full re-ingestion'
        type: boolean
        default: false

jobs:
  ingest:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm ci
      - name: Fetch upstream data
        run: npm run ingest:fetch
        env:
          USER_AGENT: 'Ansvar-MCP/1.0 (regulatory-data-ingestion)'
      - name: Check for changes
        id: diff
        run: |
          npm run ingest:diff
          echo "changed=$(cat .ingest-changed)" >> $GITHUB_OUTPUT
      - name: Rebuild database
        if: steps.diff.outputs.changed == 'true' || inputs.force
        run: |
          npm run build:db
          npm run build
          npm test
      - name: Update coverage
        if: steps.diff.outputs.changed == 'true' || inputs.force
        run: npm run coverage:update
      - name: Commit changes
        if: steps.diff.outputs.changed == 'true' || inputs.force
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/ COVERAGE.md
          git commit -m "data: automated ingestion $(date -u +%Y-%m-%d)" || true
          git push
